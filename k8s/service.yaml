apiVersion: v1
kind: Service
metadata:
  name: smartinfra-api-service
spec:
  selector:
    app: smartinfra-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
